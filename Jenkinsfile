String teststring = "no"
echo currentBuild.getBuildCauses().toString()
if (teststring == "no") {
    pipeline {
            stage('Example') {
                echo 'Hello World'
    
            }
    }
} 
