String teststring = "no"

if (teststring == "no") {
    pipeline {
            stage('Example') {
                echo 'Hello World'
                echo '${currentBuild.getBuildCauses()}'
            }
    }
} 
